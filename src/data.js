import dom from "./dom.js";

const data = {
    flags : {
        nsfw : dom.flags.nsfw.checked,
        explicit : dom.flags.explicit.checked,
        political : dom.flags.political.checked,
        sexist : dom.flags.sexist.checked,
        religious : dom.flags.religious.checked,
        racist : dom.flags.racist.checked,
    }
};
export default data;
